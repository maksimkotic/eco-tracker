SESSION_SECRET=change-me
NODE_ENV=development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eco_tracker
DB_USER=postgres
DB_PASSWORD=postgres
DB_LOGGING=false
PORT=10000
HOST=0.0.0.0

# Optional: enable real AI recommendations through OpenRouter.
# Create a fresh key in OpenRouter dashboard and set it only in your local/deploy environment.
# Do not commit real API keys to git.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/auto
OPENROUTER_APP_TITLE=Eco Tracker
OPENROUTER_SITE_URL=http://localhost:3001

# Contact form notifications
CONTACT_RECIPIENT_EMAIL=mkutov13@gmail.com
# Resend API key for sending email over HTTPS instead of SMTP.
RESEND_API_KEY=
